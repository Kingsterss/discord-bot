module.exports = {
    name: 'search',
    commands: ['search', 'srch'],
    args: [],
    description: 'Returns results from youtube search query',
    async execute(message, args, serverQueue, Discord, prefix) {

    }
}